<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gray.user.dao.PermissionDao">
	<insert id="save"  parameterType="com.gray.user.entity.Permission"  useGeneratedKeys="true" keyProperty="id">
		insert into permission(name,parent_id,sort) values(#{name},#{parentId},#{sort})
	</insert>
	<select id="listByRoleId" resultType="com.gray.user.entity.Permission" parameterType="int"> 
		SELECT * from permission where id in(
		SELECT permission_id FROM `role_permission` WHERE role_id=#{roleId}) and parent_id is null
	</select>
	<select id="listPermissionByRoleId" resultType="com.gray.user.entity.Permission" parameterType="int"> 
		SELECT * from permission where id in(
		SELECT permission_id FROM `role_permission` WHERE role_id=#{roleId}) and parent_id is not null
	</select>
</mapper>